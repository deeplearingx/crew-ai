# Core testing
pytest>=7.0.0

# Environment configuration
python-dotenv>=1.0.0

# FastAPI backend and web server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# DevCrew / CrewAI multi-agent framework
crewai>=0.100.0
crewai-tools>=0.33.0

# Config parsing
PyYAML>=6.0.0

# Browser terminal support
# Used by terminal_server.py. Windows-only PTY backend.
pywinpty>=2.0.0; platform_system == "Windows"
